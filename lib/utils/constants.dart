import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF2C4541); // Verde mexicano
  static const Color secondary = Color(0xFF4CB097); // Naranja terracota
  static const Color accent = Color(0xFFC55959); // Rojo mexicano
  static const Color background = Color(0xFFF5F5F5);
}

class AppTextStyles {
  static const TextStyle title = TextStyle(
    fontSize: 24,
    fontWeight: FontWeight.bold,
    color: Colors.white,
  );
}
